// É necessário alterar o nome deste arquivo para "constants.dart" e alterar os valores dos campos a seguir

import 'package:flutter/material.dart';

class Constants 
{
  static const apiKey = ''; // Chave API
  static const images = ''; // URL das imagens

  // static const backgroundColor = Color(hexa da cor que preferir);
  // static const ratingColor = Color(hexa da cor que preferir);

  static const currentLanguage = 'en'; //Abreviação do nome da língua atual 
}